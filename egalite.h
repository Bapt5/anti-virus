#ifndef EGALITE
#define EGALITE

#include "librairies.h"

/**
    Paramètres:
        - a: un pointeur vers un entier
        - b: un pointeur vers un entier

    Retourne:
        - true si *a == *b, false sinon
*/
bool egalite_int(int*, int*);

/**
    Paramètres:
        - a: un pointeur vers un entier
        - b: un pointeur vers un entier

    Retourne:
        - true si *a == *b, false sinon
*/
bool egalite_unsigned_long_long(unsigned long long*, unsigned long long*);

#endif